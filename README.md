Sources for the plugin `plugin.dynamic_shader`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.dynamic_shader" = {
            publisherId = "com.sporkfin",
        },
    },
}
```
